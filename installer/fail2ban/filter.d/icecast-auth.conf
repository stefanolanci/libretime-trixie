# Fail2ban filter — Icecast HTTP access log (admin / auth failures)
# Matches lines like (Icecast combined access log):
#   203.0.113.10 - - [22/Apr/2026:02:31:55 +0200] "GET /admin/ HTTP/1.1" 401 360

[Definition]

failregex = ^<HOST> - - \[[^\]]*\] "(GET|POST) /admin[^"]*" 401\b

ignoreregex =
