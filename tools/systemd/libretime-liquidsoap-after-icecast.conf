# Drop-in: ensure Icecast is up before Liquidsoap connects (avoids refused mount on boot).
# Install: /etc/systemd/system/libretime-liquidsoap.service.d/after-icecast.conf
# Then: systemctl daemon-reload && systemctl restart libretime-liquidsoap
[Unit]
After=icecast2.service
Wants=icecast2.service
